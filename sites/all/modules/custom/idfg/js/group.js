(function ($) {    Drupal.behaviors.idfg = {    attach: function (context, settings) {      var url = window.location.pathname;      // Create regexp to match current url pathname and remove trailing       // slash if present as it could collide with the link in navigation in case      // trailing slash wasn't present.      urlRegExp = new RegExp(url.replace(/\/$/,'') + "$");       // Grab every link from the navigation.      $('.group-tabs li a').each(function(){        // Test its normalized href against the url pathname regexp.        if (urlRegExp.test(this.href.replace(/\/$/,''))) {      		$(this).parent('li').addClass('active');	    	}  else {			    $(this).parent('li').removeClass('active');		    }      });  	}  }})(jQuery);